namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordGlobalTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"GlobalToken( [ {Start}..{End} ] )";

        var text = $"GlobalToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}