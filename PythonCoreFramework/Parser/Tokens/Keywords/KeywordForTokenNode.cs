namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordForTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"ForToken( [ {Start}..{End} ] )";

        var text = $"ForToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}