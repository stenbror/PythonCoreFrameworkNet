namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordOrTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"OrToken( [ {Start}..{End} ] )";

        var text = $"OrToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}