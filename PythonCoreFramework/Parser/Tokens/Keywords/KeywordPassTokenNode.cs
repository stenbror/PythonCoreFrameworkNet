namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordPassTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"PassToken( [ {Start}..{End} ] )";

        var text = $"PassToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}