namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordWithTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"WithToken( [ {Start}..{End} ] )";

        var text = $"WithToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}