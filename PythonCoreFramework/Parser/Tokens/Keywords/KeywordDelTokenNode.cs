namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordDelTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"DelToken( [ {Start}..{End} ] )";

        var text = $"DelToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}