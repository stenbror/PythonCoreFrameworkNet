namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordNotTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"NotToken( [ {Start}..{End} ] )";

        var text = $"NotToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}