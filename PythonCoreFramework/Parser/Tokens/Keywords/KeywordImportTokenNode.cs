namespace PythonCoreFramework.Parser.Tokens.Keywords;

public sealed class KeywordImportTokenNode(uint start, uint end, Trivia.Trivia[] trivia) : Token(start, end, trivia)
{
    public override string ToString()
    {
        if (Trivia.Length == 0) return $"ImportToken( [ {Start}..{End} ] )";

        var text = $"ImportToken( [ {Start}..{End} ] : [";
        foreach (var trivia in Trivia) text += $" {trivia}";
        text += $" ] )";
        return text;
    }
}